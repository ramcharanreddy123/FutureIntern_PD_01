1.Step-by-Step Guide:
->Create the Python File Locally:
->Open a text editor (such as VSCode, Sublime Text, or even Notepad).
->Copy the following code and paste it into your text editor:

#Code
def add(x, y):
    """Add two numbers"""
    return x + y

def subtract(x, y):
    """Subtract two numbers"""
    return x - y

def multiply(x, y):
    """Multiply two numbers"""
    return x * y

def divide(x, y):
    """Divide two numbers"""
    if y == 0:
        raise ValueError("Cannot divide by zero!")
    return x / y

def calculator():
    print("Basic Calculator")
    print("1. Addition")
    print("2. Subtraction")
    print("3. Multiplication")
    print("4. Division")

    choice = input("Enter your choice (1/2/3/4): ")

    num1 = float(input("Enter first number: "))
    num2 = float(input("Enter second number: "))

    if choice == '1':
        print("Result: ", add(num1, num2))
    elif choice == '2':
        print("Result: ", subtract(num1, num2))
    elif choice == '3':
        print("Result: ", multiply(num1, num2))
    elif choice == '4':
        print("Result: ", divide(num1, num2))
    else:
        print("Invalid choice")

calculator()

2. Save the File Locally:
->Save the file as calculator.py in a folder on your computer.

3. Upload the File to GitHub:
->Go to your GitHub repository or create a new one.
->Click on Add file > Create new file.
->In the file name section, type calculator.py (or any file name you prefer).
->In the large input area (under "Enter file contents here"), paste the Python code you wrote.
->Scroll down, add a commit message like Added basic calculator program, and then click Commit new file.

